from django.apps import AppConfig


class MarketsConfig(AppConfig):
    """Configuration for the markets app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.markets"
    verbose_name = "Markets"
